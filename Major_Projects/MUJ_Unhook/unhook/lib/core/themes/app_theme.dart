import '../constants/app_colors.dart';
import 'package:flutter/material.dart';

class AppTheme {
  // ignore: non_constant_identifier_names
  static ThemeData yellow_Theme = ThemeData(
    // Yellow Color theme

    // Colors
    primaryColor: AppColors.yellow,
  );
}
